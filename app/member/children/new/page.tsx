import { PhasePlaceholder } from "@/components/ui/phase-placeholder";

export const metadata = { title: "Add Child" };

export default function NewChildPage() {
  return <PhasePlaceholder title="Add Child" phase={4} />;
}
