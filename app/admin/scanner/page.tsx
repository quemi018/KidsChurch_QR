import { PhasePlaceholder } from "@/components/ui/phase-placeholder";

export const metadata = { title: "Scanner" };

export default function ScannerPage() {
  return <PhasePlaceholder title="Scanner" phase={7} />;
}
