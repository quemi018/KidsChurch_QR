import { PhasePlaceholder } from "@/components/ui/phase-placeholder";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return <PhasePlaceholder title="Settings" phase={3} />;
}
