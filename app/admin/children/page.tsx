import { PhasePlaceholder } from "@/components/ui/phase-placeholder";

export const metadata = { title: "Children" };

export default function ChildrenPage() {
  return <PhasePlaceholder title="Children" phase={4} />;
}
